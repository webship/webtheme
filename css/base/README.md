# Base styles

Foundational styles: CSS custom properties (`variables`), element resets/typography (`base`), utility classes (`utility`), and shared media-query breakpoints. Compiled from the `*.pcss.css` sources.
