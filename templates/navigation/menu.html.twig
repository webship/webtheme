{#
/**
 * @file
 * Webtheme's theme implementation for the main menu.
 *
 * Delegates the markup to the webtheme:menu SDC. See that component for
 * the full variable documentation.
 *
 * @ingroup themeable
 */
#}
{% include 'webtheme:menu' with { menu_name: menu_name, items: items } only %}
