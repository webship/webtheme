{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Delegates the markup to the webtheme:breadcrumb SDC.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% include 'webtheme:breadcrumb' with { breadcrumb: breadcrumb } only %}
