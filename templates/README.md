# Twig templates

Twig theme overrides, grouped by subject (block, content, field, form, layout, navigation, views, …). Templates that have an SDC equivalent delegate their markup to a component in `../components/` via `{% include 'webtheme:<name>' %}` / `{% embed %}`; the rest are plain overrides.
