# Automated tests

webship-js (Playwright + Cucumber-js) functional acceptance tests. `features/` holds the Gherkin scenarios, `step-definitions/` the custom steps, `selectors/` the named-selector presets, and `reports/`/`screenshots/`/`videos/` receive generated run output (git-ignored). Run with `yarn test` (see `cucumber.js`).
