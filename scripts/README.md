# Build scripts

Node.js build tooling for the theme. `css/` contains the PostCSS pipeline that compiles `*.pcss.css` sources to the committed `*.css` (run via `yarn theme:build` / `yarn theme:watch`).
