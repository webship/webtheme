# Optional config

Block placement configuration installed only when its dependencies (the target regions/blocks) are available. These `block.block.webtheme_*.yml` files place the default blocks (branding, menus, breadcrumb, messages, page title, search, footer, social) into Webtheme's regions.
