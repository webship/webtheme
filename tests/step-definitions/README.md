# Custom step definitions

Project-specific Cucumber step definitions (`webtheme.steps.js`) — login as a configured user, provision test users, assert the active theme/regions/assets, no-JS-errors, brand colour, SDC rendering, jQuery absence, and the HTMX toggles. Generic UI steps come from the installed `webship-js` package.
