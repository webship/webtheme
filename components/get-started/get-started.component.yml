$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: Get started
status: stable
description: >
  Onboarding panel shown on the front page until the first piece of
  content is published. Static markup (Drupal community links + Get
  Started + Next Steps). Wrapped in .text-content so the theme's
  text-content.css styles apply.
props:
  type: object
  properties: {}
