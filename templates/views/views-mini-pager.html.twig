{#
/**
 * @file
 * Theme override for a views mini-pager.
 *
 * Delegates the markup to the webtheme:views-mini-pager SDC.
 *
 * @see template_preprocess_views_mini_pager()
 */
#}
{% include 'webtheme:views-mini-pager' with { heading_id: heading_id, items: items } %}
