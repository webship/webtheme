# JavaScript behaviors

Vanilla-JavaScript Drupal behaviors for the theme — navigation, sticky header, search toggle, tabs, messages, comments, checkbox theming, and the opt-in HTMX glue (`htmx-init.js`). No jQuery: the theme attaches no `core/jquery` dependency. Files are referenced from `webtheme.libraries.yml`.
