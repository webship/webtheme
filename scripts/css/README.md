# PostCSS build pipeline

The PostCSS compile pipeline: `postcss-build.js` (entry), `compile.js` (transform), `changeOrAdded.js` / `check.js` (file handling), `postcss-watch.js` (watch mode), and `log.js`. Compiles every `css/**/*.pcss.css` to its sibling `*.css`.
