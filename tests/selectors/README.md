# Named selectors

JSON presets mapping human-readable names to CSS/XPath selectors (Drupal core/Claro, Drupal CMS/Gin, and Webtheme), loaded by `cucumber.js` so scenarios can reference elements by name.
