# Theme configuration

Configuration shipped with the theme. `install/` holds config imported when the theme is installed, `optional/` holds config installed only when its dependencies are met, and `schema/` defines the config schema for the theme settings.
