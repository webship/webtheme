# Theme styles

Styling injected into rich-text contexts — the CKEditor framed editor (`ckeditor-frame`) and the text-format filter guidelines (`filter.theme`). Compiled from the `*.pcss.css` sources.
