# Component styles

Per-component stylesheets (block, button, form, messages, breadcrumb, pager, comments, header, page-title, table, …) loaded by the libraries declared in `webtheme.libraries.yml`. Compiled from the `*.pcss.css` sources.
