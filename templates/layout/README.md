# Layout templates

Document and region structure — `html.html.twig`, `page.html.twig`, and every `region--*.html.twig`. Regions and the social bar delegate to SDC components; the html/page shells are plain Twig.
