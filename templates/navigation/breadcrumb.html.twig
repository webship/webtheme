{#
/**
 * @file
 * Theme override for a breadcrumb trail, with the UIkit Breadcrumb component.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {{ include('webtheme:breadcrumb', {
    items: breadcrumb,
  }, with_context: false) }}
{% endif %}
