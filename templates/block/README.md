# Block templates

Block theme overrides — the generic `block.html.twig` plus per-plugin variants (system branding, powered-by, menus, search form, page title). The branding, powered-by, and page-title blocks delegate to SDC components.
