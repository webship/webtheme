# Test scenarios

Gherkin `.feature` files describing the behaviour the theme must satisfy — login per role, Webtheme is the active default theme, regions render, assets load, no JavaScript errors, brand colour, theme settings, access matrix, SDC rendering, no jQuery, and HTMX opt-in/boost.
