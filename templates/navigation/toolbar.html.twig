{#
/**
 * @file
 * Default theme implementation for the administrative toolbar.
 *
 * Delegates the markup to the webtheme:toolbar SDC.
 *
 * @todo Remove this template when https://www.drupal.org/project/drupal/issues/3174422
 * is committed.
 *
 * @see template_preprocess_toolbar()
 * @ingroup themeable
 */
#}
{% include 'webtheme:toolbar' with {
  toolbar_attributes: toolbar_attributes,
  toolbar_heading: toolbar_heading,
  tabs: tabs,
  trays: trays,
  remainder: remainder,
} %}
