{#
/**
 * @file
 * Default theme implementation for displaying a username.
 *
 * Delegates the markup to the webtheme:username SDC.
 *
 * @see template_preprocess_username()
 * @ingroup themeable
 */
#}
{% include 'webtheme:username' with { name: name, extra: extra, link_path: link_path } %}
